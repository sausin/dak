<?xml version="1.0" encoding="utf-8"?>
<!-- FileProvider paths for the composer's camera capture (authority ${applicationId}.dakfiles). -->
<paths>
    <cache-path name="camera" path="camera/" />
</paths>
