<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Phones without telephony (tablets) can still install; SMS features stay off there. -->
    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />

    <!-- SMS / MMS as the default SMS app. (There is no WRITE_SMS permission: writes are granted by the role.) -->
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_MMS" />
    <uses-permission android:name="android.permission.RECEIVE_WAP_PUSH" />

    <!-- Subscriptions (multi-SIM), SIM numbers, contacts. -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_NUMBERS" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- Notifications, scheduled sends, background work. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- MMS transport (the platform MmsService brings up the carrier MMS APN on our behalf). -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />

    <application>

        <!-- Default SMS app: incoming SMS. Only the system (holding BROADCAST_SMS) can deliver. -->
        <receiver
            android:name="app.dak.telephony.sms.SmsDeliverReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter>
                <action android:name="android.provider.Telephony.SMS_DELIVER" />
            </intent-filter>
        </receiver>

        <!-- Default SMS app: incoming MMS notifications and reports. -->
        <receiver
            android:name="app.dak.telephony.mms.WapPushDeliverReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_WAP_PUSH">
            <intent-filter>
                <action android:name="android.provider.Telephony.WAP_PUSH_DELIVER" />
                <data android:mimeType="application/vnd.wap.mms-message" />
            </intent-filter>
        </receiver>

        <!-- Default SMS app: "Respond via message" from the incoming-call screen. -->
        <service
            android:name="app.dak.telephony.sms.HeadlessSmsSendService"
            android:exported="true"
            android:permission="android.permission.SEND_RESPOND_VIA_MESSAGE">
            <intent-filter>
                <action android:name="android.intent.action.RESPOND_VIA_MESSAGE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="sms" />
                <data android:scheme="smsto" />
                <data android:scheme="mms" />
                <data android:scheme="mmsto" />
            </intent-filter>
        </service>

        <!--
            Default SMS app changed (sent to the old and the new default, Android 7+). Exported so the platform can
            deliver it; the action is a protected broadcast and the receiver re-reads the role instead of trusting it.
        -->
        <receiver
            android:name="app.dak.telephony.role.DefaultSmsChangedReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.provider.action.DEFAULT_SMS_PACKAGE_CHANGED" />
            </intent-filter>
        </receiver>

        <!-- Class 0 (flash) SMS: Save / Dismiss from the notification, and the dialog. Never exported. -->
        <receiver
            android:name="app.dak.telephony.sms.FlashMessageReceiver"
            android:exported="false" />
        <activity
            android:name="app.dak.telephony.sms.FlashMessageActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleTop"
            android:noHistory="true"
            android:taskAffinity=""
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <!-- Results of our own PendingIntents: never exported. -->
        <receiver
            android:name="app.dak.telephony.sms.SmsStatusReceiver"
            android:exported="false" />
        <receiver
            android:name="app.dak.telephony.mms.MmsSentReceiver"
            android:exported="false" />
        <receiver
            android:name="app.dak.telephony.mms.MmsDownloadedReceiver"
            android:exported="false" />

        <!-- Repairs send/download state after reboot or update (system broadcasts reach non-exported receivers). -->
        <receiver
            android:name="app.dak.telephony.boot.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- PDU files for the platform MmsService (per-call URI grants only). -->
        <provider
            android:name="app.dak.telephony.mms.MmsFileProvider"
            android:authorities="${applicationId}.dak.mms"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/dak_mms_file_paths" />
        </provider>

        <!-- Expedited MMS downloads run as a short foreground service below Android 12. -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />
    </application>
</manifest>
